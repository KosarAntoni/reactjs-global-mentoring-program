import styled from '@emotion/styled'

export const StyledNotFound = styled.div`
  display: flex;
  flex-direction: column;
  justify-content: space-between;

  min-height: 100vh;

  h2 {
    font-size: 10rem;
    color: var(--pastele-red);
    text-align: center;
  }
`

export const StyledNotFoundHeading = styled.h2`
  font-size: 10rem;
  color: var(--pastele-red);
  text-align: center;
`
